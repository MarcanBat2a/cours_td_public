# Projet « tableau de bord », livré par un collègue parti en congé.
# Deux lignes contiennent une erreur volontaire. L'ordre des erreurs peut varier :
# lisez le message d'uv, corrigez le problème indiqué, puis relancez.
[project]
name = "tableau-de-bord"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cowsay==999.0",
    "richh==15.0.0",
]
